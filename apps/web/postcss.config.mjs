// Tailwind CSS v4 is CSS-first: the PostCSS plugin is the only config needed.
// There is intentionally NO tailwind.config.js — design tokens live in globals.css.
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
